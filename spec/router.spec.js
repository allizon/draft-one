import Vue from 'vue';
import VueRouter from 'vue-router';
import D1App from '../dist/draft-one.js';

describe('App.vue', () => {
  it('Works some magic', () => {
    // const app = Vue.extend(D1App);
    // console.log(app.router);
    expect(true).toBe(true);
  });
});
